<resources>
    <!-- Base app theme; actual colors/typography come from Compose (ui/theme/Theme.kt).
         This XML theme mainly exists to set the window background before Compose
         takes over, avoiding a white flash on launch. -->
    <style name="Theme.FinanceTracker" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">#1A1A1A</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
