<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.FinanceTracker" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">#0B2A4A</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Android 12+ system splash uses the app logo instead of the old bar-chart glyph. -->
    <style name="Theme.FinanceTracker.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_logo_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.FinanceTracker</item>
    </style>
</resources>
